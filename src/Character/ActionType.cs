namespace ProjectAzura.src.Character
{
    /// <summary>
    /// Used to determine what a given crew member can do.
    /// </summary>
    public enum ActionType
    {
        Move,
        Attack,
        Brace,
        Repair,
    }
}